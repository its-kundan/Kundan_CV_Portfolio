// next.config.mjs
/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['assets.aceternity.com','upload.wikimedia.org', 'images.unsplash.com']
    },
  };
  
  export default nextConfig;
  